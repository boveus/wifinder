
<h1> Device Show Page </h1>
<h3> ID: <%= device.id %> </h3>
<p> MAC: <%= device.mac_addr %> </p>
SSIDs: <ul>
        <% device.ssids.each do |ssid| %>
          <li><%= ssid.name %></li>
        <% end %>
      </ul>
